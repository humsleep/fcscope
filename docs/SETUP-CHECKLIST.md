# 출시 로드맵 (운영자 전용)

> 마지막 갱신: 2026-09-16. 여기 적힌 것은 **계정·키·결정**이 필요한 일이라 운영자가 직접 해야 합니다.
> 저장소: 웹 `humsleep/fconline`(앱 백엔드 `/api/v1`) · 앱 `humsleep/fcscope`(이 저장소).
> 앱은 웹 서버를 호출하므로 **웹이 먼저 배포돼 있어야** 합니다.

## 완료

| 항목 | 확인 |
|---|---|
| 웹 백엔드·계약 검증·측정 API 머지 및 배포 | ✅ `POST /api/v1/events` → `app_events` 저장 확인 |
| Supabase: 마이그레이션 0001~0020, Redirect URL, Apple provider | ✅ |
| Vercel 환경변수(넥슨·Supabase·Apple 팀 ID·AdMob 게시자·데모 닉네임) | ✅ AASA·app-ads.txt 200 |
| Apple Developer: App ID, App Group, 로그인·도메인·푸시 권한 | ✅ |
| AdMob: 앱 ID, 배너, 검색 전면광고 단위 | ✅ Info.plist 반영 |
| Google 로그인(서명 빌드) | ✅ 시뮬레이터 성공 |
| 개인정보 매니페스트(`PrivacyInfo.xcprivacy`) 본체·위젯 | ✅ |
| 출시 전 최종 감사 수정(심사 차단·크래시·보안·UX) | ✅ 웹 main `0627bd4` 배포, 없는 구단주 404 운영 확인 |
| 개인정보처리방침 v3(시행일 2026-09-23) 페이지 반영 | ✅ 공지만 남음 |

---

## 0단계 — 오늘: DB 마이그레이션 2개 (코드는 이미 배포됨)

Supabase → SQL Editor. **순서대로**, 각 파일 내용을 붙여넣고 실행합니다(웹 저장소 `supabase/migrations/`).

1. 먼저 확인: `select count(*) from profiles where char_length(nickname) not between 2 and 16;` → **0** 이어야 함
2. `0021_column_update_grants.sql` 실행 → 1번이 0이었으면 파일 맨 끝 주석의 `alter table ... validate constraint ...` 도 실행
3. `0022_drop_public_squads_read.sql` 실행
4. `scripts/verify-rls.sql` 실행 → ③ 항목 전부 ✅
5. 앱(또는 웹)에서 로그인 → 닉네임 등록 → 글 1개·댓글 1개 → 마이페이지 "내 스쿼드" 보이는지 확인

## 1단계 — 오늘: 공지 (출시일을 정하는 일)

약관·방침에 **"변경 시 시행일 7일 전 서비스 내 공지"** 약속이 있습니다.
이번에 방침에 "앱 이용 기록 · 180일"을 추가했는데, 방침 페이지 시행일은 아직 2026-09-10 그대로입니다.

1. ~~방침 페이지 시행일 2026-09-23(v3)로 변경~~ ✅ 배포 완료
2. https://www.fcscope.xyz/admin 에 **관리자 계정으로 로그인**한 뒤 "공지 배너"에 아래 문안을 붙여넣고, 링크 칸에 `/privacy` 를 넣어 게시합니다.
   (**9/16 오늘** 게시해야 9/23 시행이 7일 전 공지가 됩니다. 늦어지면 방침 시행일도 게시일+7일로 바꿔야 해요. 공지는 200자 제한이라 아래 문안은 그 안에 맞췄습니다.)

   ```
   개인정보처리방침이 2026년 9월 23일자로 개정됩니다. iOS 앱 출시에 맞춰 앱 이용 기록(익명 통계), 푸시 알림 토큰과 알림용 구단주명·즐겨찾기, 투표 중복 방지값, 알림·사진 추가 권한을 수집 항목에 추가했습니다. 자세한 내용은 개인정보처리방침에서 확인해 주세요.
   ```

   > `/admin` 은 관리자 이메일로 로그인해야 열리고, 다른 사람에게는 404 로 보입니다.

→ **공개 출시는 시행일 이후**로 잡습니다. 그 전까지는 TestFlight(운영자 본인)만 쓰므로 문제없습니다.

## 2단계 — 1~2일: APNs 키 교체

1. developer.apple.com → Keys → 예전에 채팅에 붙여넣었던 키를 **Revoke**.
2. 새 키 생성(Apple Push Notifications service) → `.p8` 다운로드(한 번만 가능).
3. Vercel 환경변수 추가 (전부 **Secret**, 채팅으로 보내지 말 것):
   `APNS_KEY_ID`(10자리) · `APNS_TEAM_ID`=`68BP5NY48R` · `APNS_BUNDLE_ID`=`xyz.fcscope.app` · `APNS_PRIVATE_KEY`(.p8 전체)
   ⚠️ `APNS_SANDBOX` 는 **만들지 마세요**(운영은 비워 둠). `1` 이면 테스트 서버로 보내 실제 기기 토큰이 전부 무효 처리됩니다.
4. **Sign in with Apple 키**(계정 삭제 시 Apple 토큰 폐기, 심사 5.1.1(v)):
   Keys → + → 이름 `FC Scope SIWA` → **Sign in with Apple** 체크 → Configure → Primary App ID `xyz.fcscope.app` → Save → Continue → Register → `.p8` 다운로드.
   Vercel(Secret): `APPLE_SIWA_KEY_ID`(10자리) · `APPLE_SIWA_PRIVATE_KEY`(.p8 전체). APNs 키와 **다른 키**입니다.
5. Redeploy.
6. 재배포가 끝나면 https://www.fcscope.xyz/admin **"서버 설정 점검"** 패널에서 ✅ 인지 확인합니다.
   ⚠️ 표시는 고쳐야 할 항목입니다(특히 `APNS_SANDBOX` 는 **비어 있어야** 정상).

## 3단계 — 1~2일: App Store Connect 앱 등록

1. 앱 → 신규 앱: iOS · 이름 **FC Scope** · 한국어 · 번들 ID `xyz.fcscope.app` · SKU `fcscope-ios`. (이미 만들었으면 건너뜀)
2. 앱 정보: 부제 "FC온라인 전적·스쿼드 진단 랩", 카테고리 스포츠, 방침 URL `https://www.fcscope.xyz/privacy`,
   마케팅 URL `https://www.fcscope.xyz`(AdMob app-ads.txt 확인용).
3. 가격: 무료. 연령 등급 설문: **사용자 생성 콘텐츠 예 · 광고 예 · 무제한 웹 액세스 아니오**.
   등급 체계가 4+/9+/13+/16+/18+ 로 바뀌었으니 결과 등급은 Connect 화면에서 확인할 것.
4. **앱 개인정보** (STORE-LISTING.md ③ 표와 같은 내용 — 둘이 다르면 STORE-LISTING 기준):

| 데이터 | 용도 | 사용자 연결 | 추적 |
|---|---|---|---|
| 이메일 주소 | 앱 기능 | 예 | 아니오 |
| 이름(Apple·Google 로그인 프로필) | 앱 기능 | 예 | 아니오 |
| 사용자 ID | 앱 기능 | 예 | 아니오 |
| 사용자 콘텐츠(게시물·댓글) | 앱 기능 | 예 | 아니오 |
| 검색 기록(검색한 구단주명·즐겨찾기) | 앱 기능, 분석 | 아니오 | 아니오 |
| 기기 ID(스쿼드 배틀 중복 투표 방지) | 앱 기능 | 아니오 | 아니오 |
| 제품 상호작용 | **분석**, 제3자 광고 | 아니오 | 아니오 |
| 기기 ID(IDFA, ATT 허용 시) | 제3자 광고 | 아니오 | **예** |
| 대략적 위치(AdMob IP) | 제3자 광고 | 아니오 | 아니오 |
| 광고 데이터 | 제3자 광고 | 아니오 | 아니오 |
| 충돌·성능 데이터(AdMob SDK) | 앱 기능 | 아니오 | 아니오 |

## 4단계 — 2~3일: TestFlight 실기기 확인

1. Xcode → `FCScope.xcodeproj` → 대상 "Any iOS Device" → Product › **Archive** → Distribute › App Store Connect › Upload.
2. App Store Connect → TestFlight → 내부 테스터에 본인 추가 → 아이폰에서 TestFlight 앱으로 설치.
3. 확인 목록 (TestFlight 판은 **테스트 광고**가 나와야 정상):

- [ ] Apple 로그인 / Google 로그인 후 앱으로 복귀
- [ ] 전적 검색 → 경기·스타일·리포트·선수, 매치 리포트 슛맵 방향
- [ ] 같은 날 **3번째 직접 검색**에서 전면광고 → 닫으면 결과로 이동
- [ ] 배너 광고가 콘텐츠를 가리지 않음
- [ ] 공유 카드(전적·매치) 저장·인스타 공유
- [ ] 위젯 추가 후 값 표시
- [ ] 알림 켜기 → 푸시 수신(2단계 완료 후)
- [ ] 공유 링크(`https://www.fcscope.xyz/...`) 탭 → 앱으로 열림
- [ ] 비행기 모드에서 오류 화면·재시도
- [ ] 글쓰기·신고·차단, 금칙어가 들어간 글이 막히는지, 계정 삭제(테스트 계정으로 — Apple 계정이면 삭제 직전 Face ID가 한 번 더 뜨는 게 정상)
- [ ] 설정 › 이용약관·개인정보처리방침 링크가 앱이 아니라 브라우저로 열림
- [ ] 인스타 스토리 공유 시 카드 이미지가 붙는지(안 붙으면 알려주세요 — Meta 앱 등록 필요)
- [ ] 설정 › 손쉬운 사용 › 큰 글씨에서 화면 깨짐 없음

문제가 있으면 화면 캡처와 함께 알려주면 수정 → 빌드 번호(`CURRENT_PROJECT_VERSION`)만 올려 다시 업로드.

## 5단계 — 병행: 스토어 자료·초기 콘텐츠

- **스크린샷**: 6.9형(1320×2868) 필수. 홈·전적·매치 리포트·공유 카드·픽 랭킹 5장. (시뮬레이터 촬영은 요청하면 처리)
- **설명·키워드·프로모션 텍스트**: `docs/STORE-LISTING.md` 에 초안 — 그대로 붙여넣기
- **커뮤니티 시드 글**: 게시글 0건. `docs/STORE-LISTING.md` 아래의 운영자 글 초안 5~7개를 운영자 계정으로 게시.
- **심사용 예시 구단주명**: `보엠`이 실제로 전적이 조회되는지 확인.

## 6단계 — 시행일(9/23) 이후: 심사 제출

1. 버전 1.0.0에 TestFlight 빌드 선택, 스크린샷·설명 입력.
2. 심사 노트:

```
FC Scope는 넥슨 오픈API(https://openapi.nexon.com, 이용약관에 따른 출처 표기) 기반의 FC온라인 전적 분석 비공식 팬 서비스입니다. NEXON·EA와 제휴 관계가 없습니다.
- 전적 검색·매치 리포트·스쿼드·픽 랭킹은 로그인 없이 이용 가능합니다. 예시 구단주명: 보엠 (홈 검색창)
- 픽 랭킹·랭커 비교는 넥슨 랭커 통계를 매일 모아 표시합니다. 수집이 덜 된 날은 "준비 중" 안내가 보일 수 있습니다.
- 로그인(Apple/Google)은 커뮤니티 글쓰기에만 필요합니다. 글·댓글을 쓰려면 로그인 후 내 정보 탭에서 커뮤니티 닉네임을 먼저 등록합니다.
  계정 삭제: 내 정보 탭 > 설정 · 약관 · 계정 > 계정 > 계정 삭제 (로그인 상태에서 표시, 즉시 삭제)
- 커뮤니티: 게시 전 금칙어 필터, 게시물·댓글 신고, 사용자 차단, 신고 누적 자동 숨김, 24시간 내 운영자 검토(문의 boheme88@naver.com)
- 광고: Google AdMob 배너, 홈 검색 시 전면광고(하루 첫 검색은 광고 없음, 두 번째 검색부터 표시).
  추적 허용(ATT) 팝업은 설치 후 처음 전적 결과가 표시될 때(예: 홈에서 "보엠" 검색) 나타납니다.
- 네이티브 SwiftUI 앱: 위젯, 푸시 알림, 공유 카드 생성, 유니버설 링크, Sign in with Apple.
```

3. 출시 방식은 **수동 출시** 권장 — 승인 후 원하는 시점에 공개.

## 7단계 — 출시 후 첫 2주: 측정

Supabase SQL Editor에서 `scripts/analytics.sql`(웹 저장소) 실행. `env='appstore'`만 집계됩니다.

| 볼 것 | 판단 기준 |
|---|---|
| D1 / D7 재방문율 | 대략 D1 25%↑·D7 10%↑이면 양호(업계 통상 수준, 이 앱 기준치는 첫 2주 값으로 새로 잡기) |
| 카드 만들기 → 공유 전환 | 낮은 카드 종류는 디자인 개선 대상 |
| 검색 수 vs 전면광고 수 | 광고 도입 후 검색이 급감하면 무료 횟수 조정 |
| 전적 보기별 사용량 | 안 쓰는 보기는 정리 후보 |

---

## 참고 — 나중에

- **Cloudflare 이관**: 트래픽이 Vercel 무료 한도에 근접할 때. 걸림돌은 `lib/push/apns.ts`의 `node:http2`·`node:crypto`, `lib/card/render.tsx`의 `next/og`.
- **`match_cache` → R2**: 디스크 IO 장애의 구조적 해결책. 계정 필요.
- **콜드 조회 최대 60초**: 넥슨 API 병렬 제한 때문. 인스턴스당 동시 3 상한 유지.
- **위젯 폰트**: 위젯에는 Pretendard가 번들되지 않아 한글이 시스템 폰트로 표시됨(깨지진 않음).
